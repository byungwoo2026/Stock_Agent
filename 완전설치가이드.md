# 📘 AI 주식 에이전트 — 완전 설치 가이드 (초보자용)

> 이 문서 하나만 따라하면 전체 시스템이 작동합니다.  
> 예상 소요 시간: 약 60~90분

---

## 📋 전체 작업 순서 한눈에 보기

```
[1단계] 준비물 설치        → Python, VS Code, Git 설치
[2단계] 파일 다운로드      → 코드 파일을 내 PC에 저장
[3단계] 이메일 설정        → Gmail 앱 비밀번호 발급
[4단계] 패키지 설치        → 필요한 파이썬 라이브러리 설치
[5단계] 로컬 테스트        → 내 PC에서 먼저 실행 확인
[6단계] GitHub 업로드      → 코드를 인터넷에 올리기
[7단계] Render 배포        → 외부에서 접속 가능한 웹 주소 생성
[8단계] Task Scheduler     → Windows 자동 실행 예약
```

---

## 🛠 1단계 — 준비물 설치

### 1-1. Python 설치
1. https://www.python.org/downloads 접속
2. **"Download Python 3.12.x"** 버튼 클릭
3. 설치 파일 실행 시 **반드시** 맨 아래 **"Add Python to PATH"** 체크 ✅
4. "Install Now" 클릭

**설치 확인** (설치 후 아래 진행):
- 키보드 `Windows키 + R` → `cmd` 입력 → 엔터
- 검은 창(명령 프롬프트)에 아래 입력:
```
python --version
```
→ `Python 3.12.x` 라고 나오면 성공 ✅

---

### 1-2. VS Code 설치
> VS Code = 코드를 편집하고 실행하는 프로그램 (메모장의 고급 버전)

1. https://code.visualstudio.com 접속
2. **"Download for Windows"** 클릭 → 설치
3. 설치 옵션 중 **"Add to PATH"**, **"Open with Code"** 모두 체크 ✅

---

### 1-3. Git 설치
> Git = 코드를 GitHub에 올릴 때 필요한 도구

1. https://git-scm.com/download/win 접속
2. 자동으로 다운로드 시작 → 설치 (모든 옵션 기본값 그대로)

**설치 확인:**
```
git --version
```
→ `git version 2.x.x` 나오면 성공 ✅

---

## 📁 2단계 — 파일 준비 (폴더 구성)

### 2-1. 프로젝트 폴더 만들기
1. `C:\` 드라이브에 폴더 생성: `C:\stock_agent`
   - 탐색기 열기 → C드라이브 클릭 → 우클릭 → 새 폴더 → `stock_agent`

### 2-2. 다운로드한 파일 배치
Claude에서 다운로드한 파일들을 모두 `C:\stock_agent` 폴더에 넣으세요.

```
C:\stock_agent\
├── skills.py
├── agent_core.py
├── validate_predictions.py
├── run_agent.py
├── dashboard.py
├── email_notify.py          ← (아래에서 새로 만들 파일)
├── requirements.txt
├── .env.template
├── render.yaml
├── run_evening.bat
└── run_morning.bat
```

### 2-3. VS Code로 폴더 열기
1. VS Code 실행
2. 메뉴: **파일(File) → 폴더 열기(Open Folder)**
3. `C:\stock_agent` 선택 → **"폴더 선택"**

이제 VS Code 왼쪽에 파일 목록이 보입니다 ✅

---

## 📧 3단계 — 이메일 설정 (Gmail 앱 비밀번호)

> 카카오 인증 없이 Gmail로 분석 결과를 받는 방법입니다.  
> Gmail 계정이 없다면 먼저 만드세요: https://accounts.google.com

### 3-1. Gmail 2단계 인증 활성화 (필수)
1. https://myaccount.google.com 접속 (Gmail 로그인 상태)
2. 왼쪽 메뉴 **"보안"** 클릭
3. **"2단계 인증"** → 아직 안 했다면 **활성화**

### 3-2. 앱 비밀번호 발급
1. https://myaccount.google.com/apppasswords 접속
2. **"앱 선택"** → "기타(맞춤 이름)" 선택
3. 이름 입력: `주식에이전트`
4. **"생성"** 클릭
5. **16자리 비밀번호 표시됨** → 어딘가 메모해두세요 (**이 창 닫으면 다시 못 봄!**)
   - 예시: `abcd efgh ijkl mnop` (공백 제외하고 사용)

### 3-3. email_notify.py 파일 생성
VS Code에서 `C:\stock_agent\email_notify.py` 파일을 **새로 만들고** 아래 내용을 붙여넣으세요:

```python
"""
email_notify.py — Gmail SMTP 이메일 알림 모듈
인증 없이 바로 사용 가능 (앱 비밀번호만 있으면 됨)
"""

import smtplib
import datetime
import os
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart
from dotenv import load_dotenv

load_dotenv()

def send_email(report_summary: dict, dashboard_url: str = "", subject_prefix: str = "📊") -> bool:
    """
    Gmail SMTP로 분석 결과 이메일 발송.
    .env 파일에서 설정값을 읽어옵니다.
    """
    sender    = os.getenv("EMAIL_SENDER")      # 보내는 Gmail 주소
    password  = os.getenv("EMAIL_APP_PASSWORD") # 앱 비밀번호 (공백 없이)
    recipient = os.getenv("EMAIL_RECIPIENT")    # 받는 이메일 주소

    if not all([sender, password, recipient]):
        print("❌ .env 파일에 EMAIL_SENDER / EMAIL_APP_PASSWORD / EMAIL_RECIPIENT 를 설정하세요.")
        return False

    today = datetime.date.today().strftime("%Y년 %m월 %d일")
    top   = report_summary.get("top_picks", [])

    # ── HTML 이메일 본문 ────────────────────────────────
    rows = ""
    for i, pick in enumerate(top[:5], 1):
        score     = pick.get("score", 0)
        bar_width = int(score / 10 * 100)
        rows += f"""
        <tr style="border-bottom:1px solid #2e3150;">
          <td style="padding:12px 16px;color:#94a3b8">{i}</td>
          <td style="padding:12px 16px">
            <strong style="color:#e2e8f0">{pick['name']}</strong><br>
            <span style="color:#64748b;font-size:12px">{pick['code']}</span>
          </td>
          <td style="padding:12px 16px">
            <span style="background:#1e3a5f;color:#60a5fa;padding:3px 10px;
                         border-radius:99px;font-size:12px">{pick.get('theme','-')}</span>
          </td>
          <td style="padding:12px 16px">
            <strong style="color:#6366f1">{score}/10</strong>
            <div style="background:#2e3150;border-radius:3px;height:4px;margin-top:4px;width:80px">
              <div style="background:#6366f1;width:{bar_width}%;height:4px;border-radius:3px"></div>
            </div>
          </td>
          <td style="padding:12px 16px;color:#94a3b8;font-size:13px">{pick.get('entry','-')}</td>
          <td style="padding:12px 16px;color:#ef4444;font-size:13px">{pick.get('stop_loss','-')}</td>
        </tr>"""

    dashboard_btn = f"""
    <div style="text-align:center;margin:28px 0">
      <a href="{dashboard_url}" style="background:#6366f1;color:#fff;padding:12px 32px;
         border-radius:8px;text-decoration:none;font-weight:600;font-size:14px">
        🔗 웹 대시보드 열기
      </a>
    </div>""" if dashboard_url else ""

    html_body = f"""
    <div style="background:#0f1117;color:#e2e8f0;font-family:'Segoe UI',sans-serif;
                max-width:700px;margin:0 auto;border-radius:12px;overflow:hidden">

      <!-- 헤더 -->
      <div style="background:#1a1d2e;padding:24px 28px;border-bottom:1px solid #2e3150">
        <h1 style="margin:0;font-size:20px;color:#6366f1">📊 AI 수급 주도주 분석</h1>
        <p style="margin:6px 0 0;color:#64748b;font-size:13px">{today} · AI 수급 전문 애널리스트 에이전트</p>
      </div>

      <!-- 요약 지표 -->
      <div style="display:flex;gap:0;border-bottom:1px solid #2e3150">
        <div style="flex:1;padding:16px 20px;border-right:1px solid #2e3150;text-align:center">
          <div style="font-size:11px;color:#64748b;margin-bottom:4px">추천 종목</div>
          <div style="font-size:24px;font-weight:700;color:#3b82f6">{len(top)}개</div>
        </div>
        <div style="flex:1;padding:16px 20px;border-right:1px solid #2e3150;text-align:center">
          <div style="font-size:11px;color:#64748b;margin-bottom:4px">7일 적중률</div>
          <div style="font-size:24px;font-weight:700;color:#22c55e">
            {report_summary.get('hit_rate_7d','N/A')}
          </div>
        </div>
        <div style="flex:1;padding:16px 20px;text-align:center">
          <div style="font-size:11px;color:#64748b;margin-bottom:4px">평균 7일 수익</div>
          <div style="font-size:24px;font-weight:700;color:#a855f7">
            {report_summary.get('avg_7d','+0.00')}%
          </div>
        </div>
      </div>

      <!-- 종목 테이블 -->
      <div style="padding:20px 0">
        <table style="width:100%;border-collapse:collapse">
          <thead>
            <tr style="border-bottom:1px solid #2e3150">
              <th style="padding:8px 16px;color:#64748b;font-size:11px;text-align:left">#</th>
              <th style="padding:8px 16px;color:#64748b;font-size:11px;text-align:left">종목</th>
              <th style="padding:8px 16px;color:#64748b;font-size:11px;text-align:left">테마</th>
              <th style="padding:8px 16px;color:#64748b;font-size:11px;text-align:left">점수</th>
              <th style="padding:8px 16px;color:#64748b;font-size:11px;text-align:left">진입구간</th>
              <th style="padding:8px 16px;color:#64748b;font-size:11px;text-align:left">손절선</th>
            </tr>
          </thead>
          <tbody>{rows}</tbody>
        </table>
      </div>

      {dashboard_btn}

      <!-- 면책 -->
      <div style="background:#1a1d2e;padding:14px 20px;border-top:1px solid #2e3150;
                  font-size:11px;color:#475569;text-align:center">
        ⚠ 본 메일은 AI 시뮬레이션 기반 참고 자료입니다. 투자 손실 책임은 투자자 본인에게 있습니다.
      </div>
    </div>"""

    # ── 메일 발송 ────────────────────────────────────────
    msg = MIMEMultipart("alternative")
    msg["Subject"] = f"{subject_prefix} [{today}] AI 수급 주도주 분석 리포트"
    msg["From"]    = sender
    msg["To"]      = recipient
    msg.attach(MIMEText(html_body, "html", "utf-8"))

    try:
        with smtplib.SMTP_SSL("smtp.gmail.com", 465) as smtp:
            smtp.login(sender, password)
            smtp.sendmail(sender, recipient, msg.as_string())
        print(f"✅ 이메일 발송 완료 → {recipient}")
        return True
    except smtplib.SMTPAuthenticationError:
        print("❌ Gmail 인증 실패. 앱 비밀번호를 확인하세요.")
        return False
    except Exception as e:
        print(f"❌ 이메일 발송 오류: {e}")
        return False


if __name__ == "__main__":
    # 테스트 실행
    dummy = {
        "top_picks": [
            {"name": "솔브레인", "code": "357780", "score": 8.6,
             "theme": "반도체장비", "entry": "310,000원", "stop_loss": "300,000원"},
            {"name": "신한지주", "code": "055550", "score": 8.1,
             "theme": "금융", "entry": "47,000원", "stop_loss": "45,500원"},
        ],
        "hit_rate_7d": "60.0%",
        "avg_7d": "+2.34",
    }
    send_email(dummy, dashboard_url="https://your-app.onrender.com", subject_prefix="🧪 테스트")
```

---

### 3-4. .env 파일 만들기
1. VS Code에서 `.env.template` 파일 클릭
2. **파일 → 다른 이름으로 저장** → 파일명 `.env` 로 저장
3. 아래처럼 수정:

```
EMAIL_SENDER=내gmail주소@gmail.com
EMAIL_APP_PASSWORD=abcdefghijklmnop
EMAIL_RECIPIENT=받을이메일@gmail.com
DASHBOARD_URL=
USE_REAL_API=false
FLASK_ENV=development
```

> `EMAIL_APP_PASSWORD`는 3-2에서 발급받은 16자리 (공백 제거)

---

## 📦 4단계 — 패키지 설치

### 4-1. VS Code 터미널 열기
VS Code 상단 메뉴: **터미널(Terminal) → 새 터미널(New Terminal)**
→ 화면 하단에 검은 창이 열립니다.

### 4-2. 폴더 이동 확인
터미널에 아래처럼 경로가 보이는지 확인:
```
C:\stock_agent>
```
다르게 나오면 아래 입력:
```
cd C:\stock_agent
```

### 4-3. 패키지 설치 실행
```
pip install -r requirements.txt
```
→ 여러 줄이 자동으로 설치됩니다 (1~3분 소요). `Successfully installed` 가 나오면 완료 ✅

---

## ✅ 5단계 — 로컬 테스트

### 5-1. 이메일 테스트
VS Code 터미널에서:
```
python email_notify.py
```
→ 받는 이메일로 테스트 메일이 오면 성공 ✅

### 5-2. 에이전트 실행 테스트
```
python run_agent.py --mode evening
```
→ 터미널에 분석 로그가 출력되고 이메일이 도착하면 성공 ✅

### 5-3. 웹 대시보드 로컬 확인
```
python dashboard.py
```
→ 터미널에 `🚀 대시보드 실행: http://localhost:5000` 출력됨  
→ 브라우저에서 **http://localhost:5000** 접속 → 대시보드 확인 ✅  
→ 확인 후 터미널에서 `Ctrl + C` 로 종료

---

## 🐙 6단계 — GitHub 업로드

> GitHub = 코드를 인터넷에 저장하는 창고. Render 배포에 필수입니다.

### 6-1. GitHub 계정 만들기
1. https://github.com 접속 → **"Sign up"**
2. 이메일 / 비밀번호 / 사용자명 입력 후 가입

### 6-2. 새 저장소(Repository) 만들기
1. GitHub 로그인 후 오른쪽 상단 **"+"** 클릭 → **"New repository"**
2. 설정:
   - Repository name: `stock-agent`
   - Visibility: **Private** (비공개 권장)
   - **README 추가 체크 해제** (비워두기)
3. **"Create repository"** 클릭
4. 만들어진 페이지에서 주소 복사:
   `https://github.com/내아이디/stock-agent.git`

### 6-3. VS Code 터미널에서 업로드
```
cd C:\stock_agent

git init
git add .
git commit -m "첫 번째 커밋: AI 주식 에이전트"
git branch -M main
git remote add origin https://github.com/내아이디/stock-agent.git
git push -u origin main
```

→ GitHub 로그인 창이 뜨면 로그인  
→ `main` 브랜치에 업로드 완료 ✅

> **⚠ 주의**: `.env` 파일은 `.gitignore`에 포함되어 있어 **자동으로 제외**됩니다.  
> 비밀번호가 인터넷에 올라가지 않으니 안심하세요.

---

## 🌐 7단계 — Render.com 배포 (웹 주소 자동 생성)

> Render = 무료로 웹사이트를 인터넷에 올려주는 서비스  
> 주소 형식: **`https://stock-agent-xxxx.onrender.com`** (자동 생성됨)

### 7-1. Render 가입
1. https://render.com 접속 → **"Get Started for Free"**
2. **"GitHub으로 로그인"** 선택 (6단계에서 만든 계정)

### 7-2. 새 웹 서비스 만들기
1. 대시보드에서 **"New +"** → **"Web Service"** 클릭
2. **"Connect a repository"** → `stock-agent` 선택 → **"Connect"**

### 7-3. 배포 설정 입력

| 항목 | 입력값 |
|------|--------|
| Name | `stock-agent` (자유) |
| Runtime | `Python 3` |
| Build Command | `pip install -r requirements.txt` |
| Start Command | `gunicorn dashboard:app` |
| Instance Type | **Free** 선택 |

### 7-4. 환경변수 입력
**"Environment Variables"** 섹션에서 아래 항목 추가:

| Key | Value |
|-----|-------|
| `EMAIL_SENDER` | 내gmail주소@gmail.com |
| `EMAIL_APP_PASSWORD` | 앱비밀번호16자리 |
| `EMAIL_RECIPIENT` | 받을이메일@gmail.com |
| `FLASK_ENV` | production |

### 7-5. 배포 시작
**"Create Web Service"** 클릭  
→ 자동으로 빌드 시작 (3~5분 소요)  
→ 상단에 **`https://stock-agent-xxxx.onrender.com`** 주소가 생성됨 ✅

### 7-6. 대시보드 URL을 .env 에 추가
생성된 주소를 복사해서:
1. VS Code에서 `.env` 파일 열기
2. `DASHBOARD_URL=https://stock-agent-xxxx.onrender.com` 입력
3. 저장 후 GitHub에 다시 push:
```
git add .env.template
git commit -m "대시보드 URL 업데이트"
git push
```
→ Render가 자동으로 재배포 ✅

---

## ⏰ 8단계 — Windows Task Scheduler 자동 실행 등록

> 매일 자동으로 분석 → 이메일 발송이 되도록 예약합니다.

### 장 마감 후 자동 실행 (오후 4:30)

1. 키보드 `Windows키` → **"작업 스케줄러"** 검색 → 실행
2. 오른쪽 **"기본 작업 만들기..."** 클릭
3. 이름: `주식에이전트_저녁` → 다음
4. 트리거: **매일** → 다음
5. 시작 시간: **오후 4:30** → 다음
6. 동작: **프로그램 시작** → 다음
7. 프로그램/스크립트:
   ```
   C:\stock_agent\run_evening.bat
   ```
8. **마침** 클릭 ✅

### 장 시작 전 자동 실행 (오전 8:50)
위 과정을 반복하되:
- 이름: `주식에이전트_아침`
- 시간: **오전 8:50**
- 파일: `C:\stock_agent\run_morning.bat`

### 등록 확인
작업 스케줄러 목록에서 등록된 작업 우클릭 → **"실행"** → 이메일 오면 성공 ✅

---

## 🔁 run_agent.py 수정 (카카오 → 이메일)

`run_agent.py` 파일을 열고 카카오 관련 코드를 이메일로 교체하세요.

**변경 전 (상단 import 부분):**
```python
from kakao_notify import send_kakao_message
```

**변경 후:**
```python
from email_notify import send_email as send_kakao_message
```

그리고 **`run_evening` 함수** 안에서:
```python
# 변경 전
success = send_kakao_message(summary, dashboard_url=DASHBOARD_URL)

# 변경 후
success = send_kakao_message(summary, dashboard_url=DASHBOARD_URL)
# (함수명이 같으므로 이 줄은 그대로 둬도 됩니다)
```

---

## 🧩 최종 파일 구성 확인

```
C:\stock_agent\
├── skills.py               ← 스킬 모듈
├── agent_core.py           ← 메인 에이전트
├── validate_predictions.py ← 복기 시스템
├── run_agent.py            ← 통합 실행기
├── dashboard.py            ← 웹 대시보드
├── email_notify.py         ← 이메일 알림 ← 새로 추가
├── requirements.txt        ← 패키지 목록
├── .env                    ← 비밀 설정 (GitHub 제외됨)
├── .env.template           ← .env 샘플
├── .gitignore              ← GitHub 제외 목록
├── render.yaml             ← Render 배포 설정
├── run_evening.bat         ← 저녁 자동실행 배치
└── run_morning.bat         ← 아침 자동실행 배치
```

---

## ❓ 자주 묻는 질문

**Q. 웹 주소는 내가 정하는 건가요, 자동인가요?**  
A. **자동 생성**됩니다. `stock-agent-[랜덤4자리].onrender.com` 형식으로 Render가 만들어줍니다. 유료 플랜으로 업그레이드하면 원하는 도메인(`내이름.com`)으로 변경 가능합니다.

**Q. Render 무료 플랜 제한이 있나요?**  
A. 있습니다. 15분 이상 접속이 없으면 서버가 절전 모드로 들어가, 다음 접속 시 30초~1분 정도 로딩이 느릴 수 있습니다. 에이전트 실행(분석)은 영향 없고 대시보드 열람 시에만 해당합니다.

**Q. 에러가 나면 어디서 확인하나요?**  
A. `C:\stock_agent\run_log.txt` 파일에 모든 실행 로그가 기록됩니다. VS Code로 열어서 확인하세요.

**Q. 실제 주식 데이터로 바꾸고 싶으면?**  
A. `.env` 파일에서 `USE_REAL_API=true` 로 변경하면 yfinance 실제 데이터로 전환됩니다.

---

> 📌 설치 중 막히는 단계가 있으면 해당 단계 번호와 오류 메시지를 알려주세요.
